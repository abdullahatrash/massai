import { ContractScaffoldPage } from "./ContractScaffoldPage";

export function ContractDocumentsPage() {
  return (
    <ContractScaffoldPage
      body="The documents route is wired into the contract workspace so evidence browsing can land cleanly in a later ticket."
      eyebrow="Documents"
      title="Documents page placeholder"
    />
  );
}
