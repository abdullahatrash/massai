FROM node:20-alpine

ENV PNPM_HOME="/pnpm"
ENV PATH="$PNPM_HOME:$PATH"

RUN corepack enable && corepack prepare pnpm@latest --activate

WORKDIR /app

EXPOSE 3000

CMD ["sh", "-lc", "pnpm install && pnpm run dev -- --host 0.0.0.0 --port ${PORT:-3000}"]
